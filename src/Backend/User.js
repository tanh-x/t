class User{
    constructor(table, name, status, duration) {
        this.table = table
        this.name = name;
        this.status = status;
        this.duration = duration
    }
}

export default User